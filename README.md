# React JWT Token login
Using react-router, and react-toolbox

# Install
`git clone https://github.com/yarnball/JWTAuth.git`

`npm install`

`npm start`

# To Do
Change to Webpack 2.
Make a build production config